<table class="table text-center">
  <thead>
    <tr>
      <th class="text-center">Email</th>
      <th class="text-center">Frequency</th>
      <th class="text-center">Rain</th>
      <th class="text-center">High</th>
      <th class="text-center">Low</th>
      <th class="text-center">Location</th>
    </tr>
  </thead>
  <tbody>
    <% @subscriptions.each do |subscription| %>
      <tr>
        <td><%= subscription.email %></td>
        <td><%= subscription.frequency %></td>
        <td><%= subscription.rain %></td>
        <td><%= subscription.high %></td>
        <td><%= subscription.low %></td>
        <td><%= subscription.location.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to new_subscription_path, style: 'margin:10px' do %><div class="btn btn-primary">New Subscription</div><% end %>